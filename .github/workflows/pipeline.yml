name: CI Pipeline for Spring Boot Project

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  call-basic-validation:
    name: Basic validation
    uses: actions/reusable-workflows/.github/workflows/basic-validation.yml@main
    with:
      node-version: '20.x'
